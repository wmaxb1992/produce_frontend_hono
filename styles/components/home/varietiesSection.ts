import { StyleSheet } from 'react-native';

export const varietiesSectionStyles = StyleSheet.create({
  section: {
    marginBottom: 4,
  },
  container: {
    flexDirection: 'row',
    marginBottom: 0,
    paddingBottom: 0,
    paddingHorizontal: 16,
  },
}); 